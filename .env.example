# Example environment variables for Parser Service
# Copy to .env (which is ignored by git) and set your own values.

# Option A: Full URL with key embedded (preferred for simplicity)
# EXCHANGERATE_API_URL=https://v6.exchangerate-api.com/v6/<YOUR_KEY>/latest/USD

# Option B: Separate key (URL will be constructed automatically)
# EXCHANGERATE_API_KEY=<YOUR_KEY>
# EXCHANGERATE_BASE=USD

# CoinGecko (ключ не требуется для simple/price)
# Вариант A: полный URL (уже содержит ids и vs_currencies)
# COINGECKO_FULL_URL=https://api.coingecko.com/api/v3/simple/price?ids=bitcoin,ethereum,solana&vs_currencies=usd
# Вариант B: базовый URL (ids подставятся из CRYPTO_ID_MAP)
# COINGECKO_URL=https://api.coingecko.com/api/v3/simple/price

# Parser HTTP timeout in seconds
# PARSER_HTTP_TIMEOUT=10

# Snapshot and history behavior
# Enable strict snapshot replacement (no merge with old pairs)
# PARSER_SNAPSHOT_STRICT=1
# Include ALL fiat currencies from ExchangeRate-API (instead of default EUR, GBP, RUB)
# PARSER_FIAT_ALL=1
# Disable appending to history for a run
# PARSER_HISTORY_DISABLED=1

# Auto-update behavior on app start (daily check vs last_refresh)
# Enable daily auto-update at start (default on)
# PARSER_AUTO_UPDATE_ON_START=1
